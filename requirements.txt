# RDD project -- pinned for reproducibility.
# The training instance uses the Deep Learning AMI's CUDA-enabled PyTorch build
# rather than installing torch from here; see infra/userdata.sh.

# --- pipeline (works on a laptop, CPU only) ---
pyyaml==6.0.2
pycocotools==2.0.8
imagehash==4.3.1
Pillow==11.0.0
numpy==2.1.3
pandas==2.2.3
tqdm==4.67.1
boto3==1.35.76

# --- training (instance only) ---
# Version pinned deliberately: Ultralytics changes defaults between minor
# releases, and the study reports exact numbers.
ultralytics==8.4.131

# --- optional, for the transformer control ---
# transformers==4.46.3
# timm==1.0.12
